import React from 'react';

import { LaunchGuideContent } from './launch-guide-content.component';

export default {
    title: 'Launch Guide',
    component: LaunchGuideContent,
};

const Template = () => <LaunchGuideContent />;
export const LaunchGuideContentTemplate = Template.bind({});
